//opening code to call function



var Flowers = ["roses", "lilies", "dandelions"];

var btnDiv = $("#FlowerButtons");
function captureInput(Flowers)
function Flowers.push(newFlowers)


function buildBtn(){
        for (let i = 0; i < Flowers.length; i++) {
                const element = Flowers[i];
                
        }
}

object.onclick function();{
    chosenFlower
    console.log(chosenFlower);
}

//create the API call to Giphy
        //1)create parameters for requested data
                //a)rating
                //b)still of gif
                //c)action gifs
        //2)use API key with a generic variable that can be looped through to pull each keyword


//display ten results for each item on the original array from giphy
        //

//display users input as a new button with giphy api



giphy api key 748mmYRkwy1N1kj7EL06Yg0vfcUXLffS